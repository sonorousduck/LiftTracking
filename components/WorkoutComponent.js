import * as React from 'react';
import { List } from 'react-native-paper';

const WorkoutComponent = () => {
    return (
        <List.Item
            title="1. Populate from Database"
            titleStyle={{color: "#32507A"}}
        />
    );
};

export default WorkoutComponent